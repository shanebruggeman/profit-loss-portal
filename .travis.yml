language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
  - cd parser
  - python -m unittest discover
# notifications for profit-loss-portal on slack
notifications:
  slack: profitlossportal:0zTwalLVxCxIqPbUp8fCA3Nn